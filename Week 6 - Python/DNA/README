//Checks to see if a given DNA strand (lengthy string) has a match in the database and then prints out the results
//The checking process uses substrings and STR length calculations to be more precise
